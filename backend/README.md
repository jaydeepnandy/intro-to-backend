This is an intro to backend

1. Incoming Request
2. app.js => Receives the request from user
3. routes => Checks the address (URL), and method (GRT, POST etc.)
4. controller => Performs the task (CRUDS) as asked in the request
5. response sent back to requester